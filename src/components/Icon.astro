---
export interface Props {
  name: string;
  title: string;
  isColor?: boolean;
}

const { name, title, isColor = false } = Astro.props as Props;
---

<span
  class={`${isColor ? "iconify-color" : "iconify"} ${name} inline-block align-[-0.15em]`}
  title={title}></span>
