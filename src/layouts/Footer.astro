---
import { SITE } from "#src/config.ts";

const endYear = new Date().getFullYear();
const formattedPeriod =
  SITE.startYear == endYear ? `${endYear}` : `${SITE.startYear} – ${endYear}`;
---

<footer
  class="mb-16 mt-12 text-pretty text-sm font-semibold leading-tight tracking-wide text-custom-muted max-laptop:text-center"
>
  &copy;&nbsp;{`${formattedPeriod} ${SITE.author}`}.
</footer>
