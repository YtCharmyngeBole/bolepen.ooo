---
import { SITE } from "../../config.ts";

const endYear = new Date().getFullYear();
const formattedPeriod =
  SITE.startYear == endYear ? `${endYear}` : `${SITE.startYear} – ${endYear}`;
---

<footer
  class="mb-16 mt-8 w-full text-pretty text-center text-sm font-semibold leading-tight tracking-wide text-grayext-475"
>
  &copy;&nbsp;{`${formattedPeriod} ${SITE.author}`}.
</footer>
